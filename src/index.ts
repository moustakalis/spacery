/**
 * Editor entry point.
 *
 * M0 placeholder: this file exists so the build pipeline has an entry point to
 * resolve and CI can prove the toolchain works end to end. Blocks arrive in M3,
 * at which point `wp-scripts build` picks them up automatically by scanning
 * `src/` for `block.json` files — no webpack configuration required.
 */

export const SPACERY_VERSION = '0.1.0';
