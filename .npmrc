# @wordpress/scripts resolves a large peer-dependency graph (webpack, babel,
# eslint plugins) by assuming a hoisted node_modules. pnpm's strict linking
# breaks that resolution, so hoist for this project.
node-linker=hoisted
strict-peer-dependencies=false
